import { FormLookupPipe } from './form-lookup.pipe';

describe('FormLookupPipe', () => {
  it('create an instance', () => {
    const pipe = new FormLookupPipe();
    expect(pipe).toBeTruthy();
  });
});
